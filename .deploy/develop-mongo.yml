releaseName: mongo
subDomain: develop
domain: atv.spectrumtoolbox.com
useAuth:
   enabled: false
image:
  repository: ${CI_REGISTRY_IMAGE}/develop-mongo
  tag: ${CI_COMMIT_REF_SLUG}
  pullPolicy: Always
cleanup:
  enabled: false
replicaCount: 1